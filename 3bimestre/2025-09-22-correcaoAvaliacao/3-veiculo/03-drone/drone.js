class Drone {
    constructor(id, modelo, autonomiaBateria, alcanceMaximo) {
        this.id = id;                       // identificador único do drone
        this.modelo = modelo;               // modelo do drone
        this.autonomiaBateria = autonomiaBateria; // autonomia em minutos
        this.alcanceMaximo = alcanceMaximo; // alcance em metros
    }
}

